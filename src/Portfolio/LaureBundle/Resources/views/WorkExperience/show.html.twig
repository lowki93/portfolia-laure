{% extends '::base.html.twig' %}

{% block body -%}
    <h1>workExperience</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Company</th>
                <td>{{ entity.company }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ entity.type }}</td>
            </tr>
            <tr>
                <th>Yourjob</th>
                <td>{{ entity.yourJob }}</td>
            </tr>
            <tr>
                <th>Information</th>
                <td>{{ entity.information }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ entity.year }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('workexperience') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('workexperience_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
