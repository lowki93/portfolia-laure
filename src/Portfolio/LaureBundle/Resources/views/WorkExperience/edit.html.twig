{% extends '::baseAdmin.html.twig' %}

{% block content %}
    <h1>workExperience edit</h1>

    {{ form(edit_form) }}

    <a href="{{ path('workexperience') }}">Back to the list</a>

    {{ form(delete_form) }}

{% endblock %}